type Track {
    id: ID!
    name: String!
    duration: Float!
    artists: [String]!
}

type User {
    id: ID!
    name: String!
    picture: String
}

type Party {
    id: ID!
    name: String!
    creator: User!
    currentTrack: Track
    playlist: [Track]!
}

scalar JsonNode

type Query {
    me: User!
    search(term: String): JsonNode
    party(id: ID!): Party
    track(id: ID!): Track
}

type Mutation {
    createParty(name: String!): Party
    addTrack(partyID: ID!): Track
}